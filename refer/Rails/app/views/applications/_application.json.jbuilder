json.extract! application, :id, :application_id, :applicant_name, :datetime, :property_name, :approved_rejected_processing, :created_at, :updated_at
json.url application_url(application, format: :json)
